# 飞鹤 / 爱他美千瓜底表一键打标

这套脚本适用于“飞鹤六月千瓜数据底表.xls”“爱他美六月千瓜数据底表.xls”同类格式。脚本会根据笔记标题和完整文案，回填 `SPU列` 及对应品牌的内容类型列，不覆盖原始底表。

## 最简单的用法（macOS）

1. 确认千瓜底表已经跑完文案，工作簿中有包含 `UID` 和 `content` 的文案明细表。
2. 双击 `运行打标.command`，在弹出窗口中选择 `.xls` 或 `.xlsx` 底表。
3. 结果会保存到源文件旁边的 `打标结果` 文件夹：
   - `原文件名_已打标.xlsx`
   - `原文件名_已打标_命中明细.csv`

也可以把一个或多个底表直接拖到 `运行打标.command` 图标上批量处理。

## 命令行用法

```bash
python3 一键打标.py "/路径/爱他美六月千瓜数据底表.xls"
```

同时处理飞鹤和爱他美：

```bash
python3 一键打标.py \
  "/路径/飞鹤六月千瓜数据底表.xls" \
  "/路径/爱他美六月千瓜数据底表.xls"
```

指定统一输出目录：

```bash
python3 一键打标.py "/路径/底表.xls" --output-dir "/路径/结果"
```

## 必需的底表结构

主表需包含：

- `笔记标题`
- `笔记链接`
- `SPU列`
- `飞鹤内容类型` 或 `爱他美内容类型`

文案明细表需包含：

- `UID`
- `content`

脚本会从主表的“笔记链接”提取 24 位 UID，关联明细表的完整文案，并将“标题 + 文案”一起参与判定。

默认情况下，如果找不到 `UID/content` 明细表，脚本会提示“请先跑文案”并停止。如果确实只想使用标题降级打标，可显式执行：

```bash
python3 一键打标.py "/路径/底表.xls" --allow-title-only
```

## 品牌自动识别与规则隔离

脚本不根据文件名猜品牌，而是根据主表字段自动识别：

- 存在 `飞鹤内容类型` 列：使用飞鹤 SPU 规则，“本品”指飞鹤，其他品牌视为竞品。
- 存在 `爱他美内容类型` 列：使用爱他美 SPU 规则，“本品”指爱他美，其他品牌视为竞品。
- 如果不是上述两个品牌，脚本会停止并提示当前支持范围，避免套错规则。

SPU 命中多个时，比较各关键词所在文本行（上一个换行符到下一个换行符）的字符数，只保留最长行对应的一个 SPU；同一行全称与简称同时命中时优先全称。

## 规则文件

以下文件需与 `一键打标.py` 放在同一目录：

- `keyword_tagger.py`：核心打标逻辑
- `竞品打标关键词拆分.xlsx`：基础关键词规则
- `关键词打标配置.json`：品牌 SPU、别名、内容类型词库及优先级

修改关键词时优先编辑 `关键词打标配置.json`，通常无需改 Python 代码。

## 环境要求

- Python 3.9 或更高版本，不需要安装 pip 包。
- `.xls` 输入需要 LibreOffice；`.xlsx` 可直接读取。
- macOS 首次运行如被系统拦截，可在终端执行：

```bash
chmod +x 运行打标.command 一键打标.py keyword_tagger.py
```
